name: "Bootstrap"
description: "Common boilerplate setup for workflows"

runs:
  using: "composite"
  steps:
    - name: Set up pnpm and Node.js
      uses: pnpm/setup@v3
      with:
        cache: true
        require-lockfile: true
