---
interface Props {
  className?: string;
}

const { className = "" } = Astro.props;
---

<div class={`h-px w-full max-w-4xl bg-gradient-to-r from-transparent via-white/10 to-transparent ${className}`}></div>
